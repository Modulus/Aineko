pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'Hello world!'
                sh "ls -la"
            }
        }
        stage("Build"){
          steps {
            echo "hallais!"
            sh "ls -la"
          }
        }
    }
}
